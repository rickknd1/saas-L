# ===========================================
# 🔐 CONFIGURATION ENVIRONNEMENT - Legal SaaS
# ===========================================

# ============================================
# 📊 DATABASE (REQUIS)
# ============================================
# URL de connexion PostgreSQL
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?sslmode=require
# Exemple Neon: postgresql://user:password@ep-xxx.eu-central-1.aws.neon.tech/neondb?sslmode=require
DATABASE_URL="postgresql://user:password@host:port/database?sslmode=require"

# ============================================
# 🔑 AUTHENTIFICATION (REQUIS)
# ============================================
# Secret JWT pour la signature des tokens (min 32 caractères)
# Générez avec: openssl rand -base64 32
JWT_SECRET="your-super-secret-jwt-key-minimum-32-characters-long"

# ============================================
# 💳 STRIPE (REQUIS)
# ============================================
# Clé secrète Stripe (commence par sk_)
STRIPE_SECRET_KEY="sk_test_..."

# Clé publique Stripe (commence par pk_)
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY="pk_test_..."

# Secret Webhook Stripe pour vérifier les événements
# Obtenez-le depuis: https://dashboard.stripe.com/webhooks
STRIPE_WEBHOOK_SECRET="whsec_..."

# IDs de prix Stripe (optionnel, mais recommandé)
STRIPE_PRICE_ID_STANDARD="price_..."
STRIPE_PRICE_ID_MONTHLY="price_..."
STRIPE_PRICE_ID_ANNUAL="price_..."

# ID produit Stripe (optionnel)
STRIPE_PRODUCT_ID="prod_..."

# ============================================
# 🌐 APPLICATION (REQUIS)
# ============================================
# URL publique de l'application
# Production: https://your-domain.com
# Développement: http://localhost:3000
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# ============================================
# ☁️ AWS S3 - STOCKAGE FICHIERS (OPTIONNEL)
# ============================================
# Si vous utilisez S3 pour stocker les documents
AWS_ACCESS_KEY_ID="your-access-key"
AWS_SECRET_ACCESS_KEY="your-secret-key"
AWS_REGION="eu-west-1"
AWS_S3_BUCKET="your-bucket-name"

# Alternative: S3-compatible (Cloudflare R2, DigitalOcean Spaces, etc.)
S3_ENDPOINT="https://your-endpoint.com"
S3_ACCESS_KEY_ID="your-access-key"
S3_SECRET_ACCESS_KEY="your-secret-key"
S3_REGION="auto"
S3_BUCKET_NAME="your-bucket-name"

# ============================================
# 🔧 DÉVELOPPEMENT
# ============================================
NODE_ENV="development"
PORT="3000"
HOSTNAME="localhost"

# ============================================
# 📝 NOTES
# ============================================
# 1. NE JAMAIS commiter le fichier .env ou .env.local !
# 2. Utilisez .env.example comme template
# 3. Créez un fichier .env.local avec vos vraies valeurs
# 4. Sur Vercel, configurez les variables dans: Settings → Environment Variables
#
# Pour générer un JWT_SECRET:
#   openssl rand -base64 32
#   OU
#   node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
